- if target.errors.any?
  #errorExplanation
  %h2
    = pluralize(target.errors.count, "erreur")
    = translate("prohibited_form_being_saved", :count => target.errors.count)
  %ul
    - target.errors.full_messages.each do |msg|
      %li= msg
  %br
