= content_for :head do
  %title
    = t(:title_pictures_show)
    \-
    = t(:application_name)

#left
  = image_tag get_picture_url(@picture.photo, :normal)

#right
  #map
    Map
  #navigation
    Navigation
  #exif
    = render :partial => "exif"