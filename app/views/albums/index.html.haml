= content_for :head do
  %title
    = t(:application_name)
    \-
    = t(:title_application_index)

- @albums.each do |album|
  .albumsListThumb
    %p
      %a{:href => album_path(album)}
        = image_tag get_picture_url(album.pictures.first.photo, :thumb)
        %br
        %span= album.name
